待完成：
	按照银行列表循环计算算式的部分。
		修改银行的配置部分。添加上银行的识别标志（银行名称加上备付金账号的后六位）

	将模板文件修改到一个文件夹。新建是统一读取。

	开发计算和配置的界面。

	开发错误收集部分。以文本形式在特定地点添加错误信息。统一显示。

	按照算式对数据进行计算。

	文件配置表的增删改查操作部分。

	在excel中添加累加时的取值方法。

	在excel中修改取值方式。默认将当月所有的值都取出来。一起参加计算。输出后一起写入目标文件。需要考虑上个月和下个月的表格。


已完成：
	对算式配置表的增删改操作部分。

	对excel的取值赋值的操作部分。